# 19. Migrate format versions

Run scenario `19`. It demonstrates the parse-to-domain-to-current-format migration pattern. A new Anki adapter still requires version-specific fixtures and acceptance evidence.
