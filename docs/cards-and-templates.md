# Card generation and templates

Standard types generate one card per template. Cloze types generate one card per distinct positive `cN` marker. Template ordinals and cloze indexes are zero-based in the domain. Validation reports references to fields not defined by the note type.

