> [!CAUTION]
> This initial release was substantially generated with artificial-intelligence assistance. Independently review and validate it before important use.

# 0.1.0-alpha.1 release notes

Primary target: Anki 26.05 build `e64c6b1a`, collection schema 18, v3 scheduler, Windows 11 build 10.0.26200. Tested toolchain: .NET SDK 8.0.400.

Supported: domain creation, nested decks, note types/templates/fields/CSS, notes/tags/GUIDs, basic and cloze card generation, validated scheduling, review-log domain values, streaming media writes, deterministic native JSON, CrowdAnki-inspired subset, and guarded legacy-representation APKG round trips.

Partial: review-log reading, deck configuration, LaTeX as preserved markup, large-package memory use, and local acceptance. Unsupported: live collection writes, modern `collection.anki21b`, arbitrary schema-18 protobuf metadata, filtered decks, and complete FSRS state.

The CrowdAnki adapter is a behavioral subset, not byte-compatible. Package import can lose unsupported fields; always retain the original. Current evidence: 46 portable tests passed; merged product coverage is 92.33% line and 71.70% branch; two opt-in compatibility tests passed; Anki 26.05 accepted synthetic scheduling and media packages in an isolated backed-up collection; a clean temporary consumer installed the local NUPKG, compiled, wrote an APKG, and read it back successfully.
