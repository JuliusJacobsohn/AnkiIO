# Versioning and deprecation

AnkiIO follows Semantic Versioning. Prereleases may change APIs. Stable API removals require a major version; obsolete APIs remain for at least one minor release when practical. Format versions are independent from package versions and never silently upgrade unsupported documents.

