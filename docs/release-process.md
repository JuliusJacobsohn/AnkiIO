# Release process

Update the changelog and compatibility report; run formatting, Release build, portable/integration tests, coverage gates, docs, pack, package validation, and a clean consumer test. Run opt-in local acceptance on a safe host. Record exact versions, tests, coverage, partial/unsupported features, risks, and AI disclosure. Tag SemVer, create checksums and GitHub release artifacts, then publish NuGet only with explicit authorization and configured credentials.

