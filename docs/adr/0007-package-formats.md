# ADR 0007: Package formats

The first adapter supports legacy `collection.anki2` APKG representation because it is documented by behavior and portable. Modern `collection.anki21b` is rejected until independently validated.

