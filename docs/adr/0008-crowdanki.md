# ADR 0008: CrowdAnki compatibility

Support is an independently implemented, explicitly labeled subset inspired by public MIT-licensed behavior. Scheduling and UI-driven merge semantics are intentionally omitted.

