# ADR 0006: Media ownership

Path media stays caller-owned and is streamed; generated bytes are copied and library-owned. SHA-256 is recorded and rechecked during package writes. Package reads currently own extracted byte arrays.

