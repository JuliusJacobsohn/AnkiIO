# ADR 0002: Stable identifiers

Numeric IDs are explicit 64-bit values and note GUIDs remain separate strings. Generated IDs are process-unique; deterministic IDs require an explicit scoped source value. Adapters preserve supplied identities.

