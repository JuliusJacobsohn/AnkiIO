# ADR 0003: Time representation

Public historical timestamps use `DateTimeOffset`. Storage adapters explicitly translate Unix milliseconds/seconds and collection-relative days; no local-time conversion occurs implicitly.

