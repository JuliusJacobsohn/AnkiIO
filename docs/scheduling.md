# Scheduling

The default `AnkiScheduling.New` asks Anki to initialize a new card. This is the safe default. Preserve imported scheduling by leaving card values unchanged. Explicit state must use consistent type/queue combinations and queue-dependent due units.

Suspended and buried queues retain the underlying card type. Flags are integers 0–7. Do not synthesize review logs to make a card look mature.

