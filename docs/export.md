# Export guide

Validate first. Native JSON is deterministic and best for source control. CrowdAnki-inspired output is intentionally scheduling-lossy. APKG output includes notes, generated cards, supported scheduler fields, review rows, and media in a legacy container modern Anki can import. Always write a new file.

