# Performance

Path-based media is hashed and copied with bounded buffers. Package databases use temporary files because SQLite requires seekable random access. JSON currently materializes the domain and is appropriate for ordinary decks; very large JSON streaming is planned. Archive totals are checked before extraction.

